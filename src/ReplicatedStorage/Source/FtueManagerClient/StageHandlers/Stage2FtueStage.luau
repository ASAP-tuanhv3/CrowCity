--!strict

--[[
	Client-side handler for FTUE Stage 2.

	setup()    is called when the player enters this stage.
	teardown() is called when the player leaves (stage advances or data resets).
--]]

local Stage2FtueStage = {}

function Stage2FtueStage.setup()
	-- TODO: Add client-side guidance for Stage 2.
end

function Stage2FtueStage.teardown()
	-- TODO: Clean up anything created in setup().
end

return Stage2FtueStage
