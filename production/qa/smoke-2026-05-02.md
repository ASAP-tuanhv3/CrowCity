# Smoke Check Report
**Date**: 2026-05-02 (PM re-check; supersedes AM run)
**Sprint**: Sprint 3 — Core Spine (Round Mechanics Live)
**Engine**: Roblox (engine-ref pinned 2026-04-20)
**QA Plan**: `production/qa/qa-plan-sprint-3-2026-05-02.md`
**Argument**: `sprint`

---

## Automated Tests

**Status**: PASS (278 tests, 278 passing, 0 failing) — headless via `run-in-roblox`.

**Run command**:
```bash
rojo build test.project.json -o test-place.rbxl && \
  run-in-roblox --place test-place.rbxl --script tests/runner.server.luau
```

**Last line**: `[runner] PASS — 278 successes`

Test suite breakdown:
- Sprint 2 baseline: 159 tests (all maintained)
- Sprint 3 new: 119 tests across 16 spec files (10 must-have stories closed)

**Studio in-editor parity (resolved AM)**:
First Studio Play F5 in AM session reported `275 passed, 3 failed`. All 3 failures in `tests/unit/crowd-state-server/countchanged.spec.luau` — pre-existing Sprint 2 spec expecting BindableEvent fire-then-assert in same `it()` block.

Root cause: Studio default `Workspace.SignalBehavior=Deferred` (handlers run on next resumption point — TestEZ assertions execute before handler runs, recorded count stays 0). `run-in-roblox` headless effectively runs in Immediate mode → tests pass headless but fail in-Studio.

Fix: `test.project.json` Workspace pinned to `SignalBehavior: "Immediate"`. Production `default.project.json` retains `Deferred` (production-correct). Tests now deterministic regardless of runner. PM re-run confirms 278/0/0 in headless.

---

## Audit Gates

| Gate | Result |
|---|---|
| `selene src/` | 0 errors / 7 warnings (5 template pre-existing + 2 MSM `Network`/`RemoteEventName` reserved for 3-15 AFKToggle) / 0 parse errors |
| `tools/audit-asset-ids.sh` | PASS — no `rbxassetid://` magic strings outside `SharedConstants/AssetId.luau` |
| `tools/audit-persistence.sh` | PASS — `DataStoreService` confined to ProfileStore; no Pillar 3 forbidden keys |
| `tools/audit-no-competing-heartbeat.sh` | PASS — only TickOrch + BeamBetween + ProfileStore use `Heartbeat:Connect` |
| `tools/audit-no-currency-in-shutdown.sh` | PASS — no currency calls in shutdown chain |

## CI

`.github/workflows/tests.yml` — selene + audit-asset-ids + audit-persistence blocking on PR/push to main. TestEZ headless job warn-only on `ubuntu-latest` (Studio unavailable on Linux); self-hosted macOS runner pending — Sprint 2 advisory item 2 carried forward.

## Working Tree

HEAD at `13e3bf1` (last commit: "chore: close story 3-1 — TickOrch 004 BindToClose"). Stories 3-2..3-10 implementations dirty in working tree (not yet committed):
- `src/ServerStorage/Source/CrowdStateServer/init.luau`
- `src/ServerStorage/Source/MatchStateServer/init.luau`
- `src/ServerStorage/Source/RoundLifecycle/init.luau`
- `src/ServerStorage/Source/_PhaseStubs/RelicSystemStub.luau`
- `src/ServerStorage/Source/_PhaseStubs/CurrencyStub.luau` (new)

Identical code path to AM smoke. PM re-run re-validates same artifacts post-AM SignalBehavior fix.

---

## Test Coverage

| Story | Type | Test File(s) | Coverage Status |
|---|---|---|---|
| 3-1 TickOrch 004 BindToClose | Integration | `tests/integration/tick-orchestrator/bindtoclose_shutdown.spec.luau` | COVERED |
| 3-2 CSM 003 hue F6 + activeRelics | Logic | `hue.spec.luau` + `active_relics_cap.spec.luau` + `relic_changed_signal.spec.luau` | COVERED |
| 3-3 CSM 004 F1 radius | Logic | `radius_compose.spec.luau` + `recompute_radius.spec.luau` | COVERED |
| 3-4 CSM 006 Phase 5 + F7 | Logic | `state_evaluator.spec.luau` + `grace_timer.spec.luau` | COVERED |
| 3-5 CSM 008 Phase 8 broadcast | Integration | `broadcastall.spec.luau` (perf soak deferred) | COVERED |
| 3-6 MSM 002 driver chain | Logic | `transition_lobby_ready.spec.luau` + `transition_ready_snap.spec.luau` + `snap_to_active_call_order.spec.luau` | COVERED |
| 3-7 MSM 003 T7 + F4 tiebreak | Logic | `timer_check_t7.spec.luau` + `f4_tiebreak.spec.luau` | COVERED |
| 3-8 MSM 004 Phase 7 + double-signal | Logic | `elim_consumer_t6.spec.luau` + `double_signal_guard.spec.luau` | COVERED |
| 3-9 MSM 005 Result+Intermission ordering | Integration | `grant_before_broadcast.spec.luau` + `t9_t10_intermission_lobby.spec.luau` | COVERED |
| 3-10 RL 002 CountChanged peak F1 | Logic | `peak_tracking_f1.spec.luau` | COVERED |
| 3-11 CRB 001 mirror | Logic | — | OUT-OF-SCOPE (backlog) |
| 3-12 RL 003 Eliminated + DC freeze | Logic | — | OUT-OF-SCOPE (backlog) |
| 3-13 CSM 005 F2 position lag | Logic | — | OUT-OF-SCOPE (backlog) |
| 3-14 TickOrch 005 instrumentation | Logic | — | OUT-OF-SCOPE (backlog) |
| 3-15 MSM 007 AFKToggle 4-Check | Integration | — | OUT-OF-SCOPE (backlog) |

**Summary**: 10/10 must-have COVERED. 0 MISSING in scope. 5 backlog stories (3-11..3-15) explicitly `status: backlog` in `sprint-status.yaml` — out-of-scope for must-have gate.

---

## Manual Smoke Checks

Reusing AM validation (no commits since; identical working tree). Per `production/qa/qa-plan-sprint-3-2026-05-02.md` §Smoke Test Scope:

**Batch 1 — Core stability**:
- [x] Studio Play F5 boot clean post-SignalBehavior fix — PASS (AM)
- [x] TestEZ headless 278/0/0 re-verified PM — PASS
- [x] selene clean (0 errors) — PASS
- [x] 4 audit gates green — PASS

**Batch 2 — Sprint 3 mechanics + regression**:
- [x] MSM full transition chain Lobby→Ready→Snap→Active→Result→Intermission→Lobby — PASS via `t9_t10_intermission_lobby` + `grant_before_broadcast` + `snap_to_active_call_order`
- [x] CSM Phase 5 stateEvaluate + Phase 8 broadcastAll — PASS via `state_evaluator` + `broadcastall`
- [x] CrowdEliminated dual-fire (BindableEvent server-side → MSM Phase 7; reliable RemoteEvent → client) — PASS via `state_evaluator` + `double_signal_guard`
- [x] Phase 6 < Phase 7 ordering same-tick — PASS via `timer_check_t7` + `double_signal_guard`
- [x] Grant-before-broadcast invariant — PASS via `grant_before_broadcast`
- [x] BindToClose orderly shutdown (no grant during shutdown) — PASS via `bindtoclose_shutdown` + `audit-no-currency-in-shutdown`
- [x] Sprint 2 baseline preserved (159 tests) — PASS, no regression

**Batch 3 — Data integrity + performance**:
- [x] ProfileStore save/load — N/A this sprint (no PlayerData changes)
- [x] Headless runtime ~5s for 278 tests on macOS — PASS
- [-] Phase 5/8 60s soak in Studio — DEFERRED (story 3-5 §AC-17 → MVP-Integration-1 per ADR-0003 §Validation Sprint Plan)
- [x] No new frame hitches — N/A (server-only sprint, no client gameplay loop)

---

## Missing Test Evidence

None in must-have scope.

**Deferred manual evidence** (story 3-5 §AC-17/18/20 → MVP-Integration-1 sprint per ADR-0003):
- `production/qa/evidence/csm-perf-soak-evidence.md` (60s perf soak @ 12 crowds)
- `production/qa/evidence/csm-replication-correctness-evidence.md` (multi-client correctness)
- `production/qa/evidence/csm-eliminated-replication-evidence.md` (Eliminated state replication)

Not blocking for Sprint 3 close-out.

---

## Notable Findings This Session

**1. SignalBehavior parity fix held** — PM re-run confirms `test.project.json` `SignalBehavior=Immediate` patch from AM session is durable. 278/0/0 in headless. Production `default.project.json` retains `Deferred`.

**2. Pre-existing selene warnings unchanged** — MSM `init.luau` `Network` + `RemoteEventName` imports reserved for story 3-15 (AFKToggle 4-Check wiring). Carry-forward; not regression.

**3. Working tree dirty** — Stories 3-2..3-10 implementations not committed. Recommend `/code-review` per-story OR sprint close-out aggregate (lean mode) → commit cluster before `/team-qa sprint`.

---

## Verdict: **PASS**

All criteria met:
- Automated tests PASS (278/0/0 headless, re-verified PM)
- All 4 audit gates green + selene clean (0 errors)
- All Batch 1 + Batch 2 + Batch 3 smoke checks PASS or N/A
- 0 MISSING test evidence in must-have scope
- Studio parity issue resolved (AM session)
- Backlog 3-11..3-15 explicitly out-of-scope (status: backlog)

Build ready for `/team-qa sprint` manual QA cycle and Sprint 3 close-out.

---

## Next Steps

1. Commit dirty working tree (10 must-have stories' implementations) — `/code-review` per-story or aggregate, then commit cluster.
2. `/team-qa sprint` for full QA cycle + sign-off (APPROVED / APPROVED WITH CONDITIONS / NOT APPROVED).
3. After QA APPROVED: `/gate-check production` to validate Pre-Production → Production gate.
4. Optional pre-close-out: pull in Sprint 3 backlog (3-11 CRB mirror, 3-12 RL Eliminated+DC, 3-13 CSM F2 lag, 3-14 TickOrch instrumentation, 3-15 MSM AFKToggle 4-Check) if velocity allows.
5. Required before MVP integration: fill deferred manual evidence docs for story 3-5.
